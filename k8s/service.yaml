apiVersion: v1
kind: Service
metadata:
  name: luxe-living-store
  labels:
    app: luxe-living-store
spec:
  selector:
    app: luxe-living-store
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
